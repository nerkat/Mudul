PROMPT_ID: salesCall.user@v1.0.0

Analyze the following sales call transcript and return a JSON object with these fields:
- version: "1.0.0"
- callId: string (use a stable id if provided, otherwise "unknown")
- summary: 1-3 sentences
- sentiment: { overall: "positive"|"neutral"|"negative", score: number in [0,1] }
- bookingLikelihood: number in [0,1]
- objections: [{ type: string, quote: string, ts: "HH:MM:SS" }]
- actionItems: [{ owner: "sales_rep"|"prospect", text: string, due?: "YYYY-MM-DD" }]
- keyMoments: [{ label: string, ts: "HH:MM:SS" }]
- entities: { prospect?: string[], people?: string[], products?: string[] }
- complianceFlags: string[]

TRANSCRIPT: